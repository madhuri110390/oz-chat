# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.

# Build Time Optimizations
org.gradle.jvmargs=-Xmx6g -Xms1g -XX:+UseG1GC -XX:+UseStringDeduplication -XX:MaxMetaspaceSize=1024m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
org.gradle.configureondemand=true
org.gradle.parallel=true
org.gradle.vfs.watch=true
org.gradle.caching=true

# Android Settings
android.enableJetifier=true
#android.jetifier.ignorelist=android-base-common,common
android.useAndroidX=true

#Project Settings
# Change debugPrivateData to true for debugging
vector.debugPrivateData=false
# httpLogLevel values: NONE, BASIC, HEADERS, BODY
vector.httpLogLevel=BASIC

# Note: to debug, you can put and uncomment the following lines in the file ~/.gradle/gradle.properties to override the value above
#vector.debugPrivateData=true
#vector.httpLogLevel=BODY

# Dummy values for signing secrets
signing.element.storePath=/home/codenia/Documents/Production Element Code/OZ-Chat_Prod/20251125_OZ-Chat_Android_App/OZ-Chat_Android_Dev/ozchat-new-key.jks
signing.element.storePassword=ozchat123
signing.element.keyId=ozchat
signing.element.keyPassword=ozchat123

# Dummy values for signing secrets / nightly
signing.element.nightly.storePassword=ozchat123
signing.element.nightly.keyId=ozchat
signing.element.nightly.keyPassword=ozchat123

android.bundle.enableUncompressedNativeLibs=true


# Customise the Lint version to use a more recent version than the one bundled with AGP
# https://googlesamples.github.io/android-custom-lint-rules/usage/newer-lint.md.html
android.experimental.lint.version=9.0.0-alpha09
ksp.useKSP2=false
android.jetifier.ignorelist=common-31.3.2

